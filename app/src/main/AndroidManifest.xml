<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tiki">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tiki">
        <activity android:name=".Login_SignUp" />
        <activity android:name=".Clickchat.Clickchat" >
        </activity>
        <activity android:name=".giohang.Clickgiohang" />
        <activity android:name=".giohang.GioHang" />
        <activity android:name=".Chat" />
        <activity android:name=".DetailTrangChu" />
        <activity android:name=".CaNhan" />
        <activity android:name=".Luot" />
        <activity android:name=".DanhMuc" />
        <activity android:name=".TrangChu" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>